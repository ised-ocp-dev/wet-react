import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { MultimediaPlayer } from '../..';

import puppies from '@storybook/react/puppies.mp4';

export const Template = (args) => (
  <MultimediaPlayer
    sources={[
      {
        type: 'mp4',
        source: puppies,
      },
    ]}
    {...args}
  />
);

<Meta
  title="Components/MultimediaPlayer/Video/Title"
  component={MultimediaPlayer}
/>

# MultimediaPlayer

## Title

Add a `title` attribute to the video element with the value being the title of the video. It will be visible as a mouseover tooltip

## Example

<Canvas>
  <Story name="Title" args={{ title: 'this is the title' }}>
    {Template.bind({})}
  </Story>
</Canvas>
