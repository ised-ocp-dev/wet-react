import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { MultimediaPlayer } from '../..';

<Meta
  title="Components/MultimediaPlayer/Enhanced"
  component={MultimediaPlayer}
  subcomponents={{ 'MultimediaPlayer.Button': MultimediaPlayer.Button }}
/>

export const MultimediaPlayerActiveTemplate = (args) => {
  return (
    <ul class="MultimediaPlayer">
      <li>
        <a href="#" rel="prev">
          Previous
        </a>
      </li>
      <MultimediaPlayer.Button {...args}></MultimediaPlayer.Button>
      <li>
        <a href="#">
          2 <span class="wb-inv">Go to Page 2</span>
        </a>
      </li>
      <li>
        <a href="#" rel="next">
          Next
        </a>
      </li>
    </ul>
  );
};

export const MultimediaPlayerDisabledTemplate = (args) => {
  return (
    <ul class="MultimediaPlayer">
      <MultimediaPlayer.Button {...args}></MultimediaPlayer.Button>
      <li>
        <a href="#">
          1 <span class="wb-inv">Go to Page 1</span>
        </a>
      </li>
      <li>
        <a href="#">
          2 <span class="wb-inv">Go to Page 2</span>
        </a>
      </li>
      <li>
        <a href="#" rel="next">
          Next
        </a>
      </li>
    </ul>
  );
};

# MultimediaPlayer

using the `active` and `disabled` props it is possible to set the enhanced values of the `CuePointButton`

## Active

Use to indicate the current page that the user is on

<Canvas>
  <Story name="Active" args={{ active: true, disabled: false, value: '1' }}>
    {MultimediaPlayerActiveTemplate.bind({})}
  </Story>
</Canvas>

## Disabled

Use to hide unnecessary navigation, for example a "Previous" button if the user is on "Page 1" of a content section

<Canvas>
  <Story
    name="Disabled"
    args={{ active: false, disabled: true, value: 'prev' }}
  >
    {MultimediaPlayerDisabledTemplate.bind({})}
  </Story>
</Canvas>
