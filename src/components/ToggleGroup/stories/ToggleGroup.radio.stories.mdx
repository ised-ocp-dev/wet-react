import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import ToggleGroup from '../ToggleGroup';
import { Toggle } from '../../';

<Meta title="Components/ToggleGroup/Radio" component={ToggleGroup} />

export const Template = (args) => (
  <ToggleGroup {...args}>
    <Toggle name="toggles" id="id-1" value="1">
      Toggle 1
    </Toggle>
    <Toggle name="toggles" id="id-2" value="2">
      Toggle 2
    </Toggle>
    <Toggle name="toggles" id="id-3" value="3">
      Toggle 3
    </Toggle>
  </ToggleGroup>
);

# ToggleGroup

## Radio ToggleGroup

Example of a `radio` toggle group:

<Canvas>
  <Story name="Radio" args={{ type: 'radio', name: 'toggles' }}>
    {Template.bind({})}
  </Story>
</Canvas>
