import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import { Lightbox } from '../..';
import cat1 from '@storybook/react/cat_1.jpg';
import cat2 from '@storybook/react/cat_2.jpg';
import cat3 from '@storybook/react/cat_3.jpg';

<Meta
  title="Components/Lightbox/Intro"
  component={Lightbox}
  subcomponents={{
    'Lightbox.Gallery': Lightbox.Gallery,
  }}
/>

# Lightbox

Display images in a popup box, either individually or as part of a gallery.
Implements the <a href="https://www.w3.org/TR/wai-aria-practices/#dialog_modal" target="_blank">
WAI-ARIA Dialog (Modal) design pattern</a>

## Example Lightbox

<Canvas>
  <Lightbox
    title="Example Lightbox 1"
    src={cat1}
    children={<p>Example Lightbox Link</p>}
  />
  <p>below this is an image that acts as a second lightbox link</p>
  <Lightbox
    title="Example lightbox 2"
    src={cat3}
    children={
      <span>
        <img src={cat3} alt="hi"></img>
      </span>
    }
  />
</Canvas>

## Available Props

<ArgsTable />
