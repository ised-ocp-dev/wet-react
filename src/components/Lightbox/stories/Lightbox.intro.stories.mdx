import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import { Lightbox } from '../..';

<Meta
  title="Components/Lightbox/Intro"
  component={Lightbox}
  subcomponents={{
    'Lightbox.Content': Lightbox.Content,
    'Lightbox.Gallery': Lightbox.Gallery,
  }}
/>

# Lightbox

Display images and other content in a dialog box, either individually or as part of a gallery.
Implements the <a href="https://www.w3.org/TR/wai-aria-practices/#dialog_modal" target="_blank">
WAI-ARIA Dialog (Modal) design pattern</a>

## Example Lightbox

<Canvas>
  <span>
    <Lightbox
      title="Example lightbox"
      link="#link"
      children={<p>Example Lightbox Link</p>}
    />
    <Lightbox.Content id="link" children={<p>display</p>} />
    <a class="wb-lbx" title="Example of inline content" href="#inline_content">
      Inline content
    </a>
    <section
      id="inline_content"
      class="mfp-hide modal-dialog modal-content overlay-def"
    >
      <p>hello</p>
    </section>
  </span>
</Canvas>

## Available Props

<ArgsTable />
