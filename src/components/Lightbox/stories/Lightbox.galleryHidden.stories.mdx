import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Lightbox } from '../..';
import cat1 from '@storybook/react/cat_1.jpg';
import cat2 from '@storybook/react/cat_2.jpg';
import cat3 from '@storybook/react/cat_3.jpg';

<Meta
  title="Components/Lightbox/Gallery Hidden"
  component={Lightbox}
  subcomponents={{
    'Lightbox.Gallery': Lightbox.Gallery,
  }}
/>

export const TemplateTwo = (args) => (
  <Lightbox.Gallery id="uniqueID">
    <Lightbox
      title="Example lightbox 1"
      src={cat1}
      children={
        <span>
          <img src={cat1} height="300px" />
          <p>First Picture, so not hidden</p>
        </span>
      }
    />
    <Lightbox
      title="Example lightbox 2"
      src={cat2}
      children={<p>Second Picture</p>}
      {...args}
    />
    <p>
      Note the other pictures are hidden, but you can see them when going
      through the gallery
    </p>
    <Lightbox
      title="Example lightbox 3"
      src={cat3}
      children={<p>Third Picture</p>}
      {...args}
    />
  </Lightbox.Gallery>
);

# Lightbox

## Hidden

To hide lightboxes in a gallery, for example all but the first lightbox, use the `hidden` attribute in the
undesired lightboxes

## Example with hidden

<Canvas>
  <Story name="Gallery Hidden" args={{ hidden: true }}>
    {TemplateTwo.bind({})}
  </Story>
</Canvas>
