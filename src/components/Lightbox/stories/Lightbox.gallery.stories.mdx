import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Lightbox } from '../..';
import cat1 from '@storybook/react/cat_1.jpg';
import cat2 from '@storybook/react/cat_2.jpg';
import cat3 from '@storybook/react/cat_3.jpg';

<Meta
  title="Components/Lightbox/Gallery"
  component={Lightbox}
  subcomponents={{
    'Lightbox.Gallery': Lightbox.Gallery,
  }}
/>

export const Template = (args) => (
  <Lightbox.Gallery {...args}>
    <li>
      <Lightbox
        title="Example lightbox 1"
        src={cat1}
        children={<img src={cat1} height="300px" />}
      />
    </li>
    <li>
      <Lightbox
        title="Example lightbox 2"
        src={cat2}
        children={<img src={cat2} height="300px" />}
      />
    </li>
    <li>
      <Lightbox
        title="Example lightbox 3"
        src={cat3}
        children={<img src={cat3} height="300px" />}
      />
    </li>
  </Lightbox.Gallery>
);

# Lightbox

## Gallery

Use the `Lightbox.Gallery` element to make a gallery for displaying multiple lightboxes

## Example

<Canvas>
  <Story name="Gallery" args={{ id: 'unique' }}>
    {Template.bind({})}
  </Story>
</Canvas>
