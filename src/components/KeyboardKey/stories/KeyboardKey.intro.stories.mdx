import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import { KeyboardKey } from '../..';

<Meta title="Components/KeyboardKey/Intro" component={KeyboardKey} />

# Keyboard Key

Use to mimic the keys of a computer keyboard

## Example

Use `<kbd>` to describe a keyboard key that a user is to select in a process<br/>
Use the exact spelling, spacing and sentence case of the keyboard text (for example, Ctrl)

<Canvas>
  <KeyboardKey keyValue="Shift" />
  <KeyboardKey keyValue="Ctrl" title="Control" />
  <KeyboardKey keyValue="Enter" />
</Canvas>

## Available Props

<ArgsTable />
