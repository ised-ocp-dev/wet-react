import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import { FootnoteLink } from '../..';
import { FootnoteSection } from '../..';

<Meta title="Components/Footnote/Default" component={FootnoteSection} />

# Footnote

## Default Link

Make a default FootnoteLink to link to the FootnoteSection itself by leaving value empty

## Example

<Canvas>
  <span>
    <p>
      Below is a default link to the footnotes section normally found at the
      bottom of the page. Since no footnote is specified, it will link to the
      footnote section itself
    </p>
    <FootnoteLink />
    <p>And now, the footnote section itself</p>
    <FootnoteSection>
      <FootnoteSection.Footnote
        value="1"
        children={<p>Example of a standard footnote</p>}
      />
    </FootnoteSection>
  </span>
</Canvas>

## Default Point

A workaround was implemented to ensure the footer items always refer to the first instance where the footnote is refered in the content.

It is a best practice that the footers are property coded where their reference is pointing initially to the first instance.

When this is not the case, it will show a warning like: `wb-fnote - Relink first reference of #myfoootnote for #fn4-1-rf`.

This best practice can be disabled by setting the `disableDefaultPoint` attribute of a given `Footnote` to true.

## Example

<Canvas>
  <FootnoteSection>
    <FootnoteSection.Footnote
      children={<p>"This is a foot"</p>}
      value="1"
      disableDefaultPoint
    />
  </FootnoteSection>
</Canvas>

note: by including `disableDefaultPoint` in the arguments, the attribute is automatically
set to true
