import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { FootnoteLink } from '../FootnoteLink';
import { FootnoteSection, Footnote } from '../FootnoteSection';

<Meta
  title="Components/Footnote/General"
  component={FootnoteSection}
  subcomponents={{ FootnoteLink, Footnote }}
/>

<Meta
  title="Components/Footnote/Enhanced"
  component={Footnote}
  subcomponents={{ 'Footnote.Button': Footnote.Button }}
/>

export const FootnoteActiveTemplate = (args) => {
  return <p>hi</p>;
};

export const FootnoteDisabledTemplate = (args) => {
  return <ul class="Footnote"></ul>;
};

# Footnote

using the `active` and `disabled` props it is possible to set the enhanced values of the `FootnoteButton`

## Active

Use to indicate the current page that the user is on

<Canvas>
  <Story name="Active" args={{ active: true, disabled: false, value: '1' }}>
    {FootnoteActiveTemplate.bind({})}
  </Story>
</Canvas>

## Disabled

Use to hide unnecessary navigation, for example a "Previous" button if the user is on "Page 1" of a content section

<Canvas>
  <Story
    name="Disabled"
    args={{ active: false, disabled: true, value: 'prev' }}
  >
    {FootnoteDisabledTemplate.bind({})}
  </Story>
</Canvas>
