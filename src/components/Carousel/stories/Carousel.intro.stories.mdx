import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Carousel from '../Carousel';
import Image from '@components/Image';
import naturePhoto from '@storybook/react/nature.jpg';
import fieldPhoto from '@storybook/react/field.jpg';
import treesPhoto from '@storybook/react/trees.jpg';

<Meta
  title="Components/Carousel/Intro"
  component={Carousel}
  subcomponents={{
    'Carousel.Item': Carousel.Item,
    'Carousel.Caption': Carousel.Caption,
  }}
/>

# Carousel

A slideshow component for cycling through elements (e.g., images, text, etc.).

## Example

<Canvas>
  <Carousel interval={3000}>
    <Carousel.Item>
      <Image style={{ width: '100%' }} src={naturePhoto} />
      <Carousel.Caption>
        <h5>First Slide</h5>
        <p>Hills in the clouds.</p>
      </Carousel.Caption>
    </Carousel.Item>
    <Carousel.Item>
      <Image style={{ width: '100%' }} src={fieldPhoto} />
      <Carousel.Caption>
        <h5>Second Slide</h5>
        <p>Open fields.</p>
      </Carousel.Caption>
    </Carousel.Item>
    <Carousel.Item>
      <Image style={{ width: '100%' }} src={treesPhoto} />
      <Carousel.Caption>
        <h5>Third Slide</h5>
        <p>Forest road.</p>
      </Carousel.Caption>
    </Carousel.Item>
  </Carousel>
</Canvas>

## Available Props

<ArgsTable />
