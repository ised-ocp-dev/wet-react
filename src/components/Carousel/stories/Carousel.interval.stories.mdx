import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Carousel from '../Carousel';
import Image from '@components/Image';
import naturePhoto from '@storybook/react/nature.jpg';
import fieldPhoto from '@storybook/react/field.jpg';
import treesPhoto from '@storybook/react/trees.jpg';

<Meta title="Components/Carousel/Interval" component={Carousel} />

export const TemplateGroup = (args) => {
  return (
    <Carousel {...args}>
      <Carousel.Item>
        <Image style={{ width: '100%' }} src={naturePhoto} />
      </Carousel.Item>
      <Carousel.Item>
        <Image style={{ width: '100%' }} src={fieldPhoto} />
      </Carousel.Item>
      <Carousel.Item>
        <Image style={{ width: '100%' }} src={treesPhoto} />
      </Carousel.Item>
    </Carousel>
  );
};

# Carousel Interval Prop

The Carousel `interval` prop determines the amount of time to delay between automatically cycling an item. If not specified, the carousel will not automatically cycle.

## Default (No Interval)

<Canvas>
  <Story name="Default" args={{}}>
    {TemplateGroup.bind({})}
  </Story>
</Canvas>

## Carousel Interval (3 Seconds) Example

<Canvas>
  <Story name="Carousel Interval" args={{ interval: 3000 }}>
    {TemplateGroup.bind({})}
  </Story>
</Canvas>
