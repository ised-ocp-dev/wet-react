import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import { Toggle } from '../..';

<Meta title="Components/Toggle/Intro" component={Toggle} />

# Toggle

Use toggles to switch an element between states. Toggles must be nested inside a ToggleGroup component.

## Example

<Canvas>
  <Toggle value="value">Toggle</Toggle>
</Canvas>

## Available Props

<ArgsTable />
