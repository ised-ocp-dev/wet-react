import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Toggle } from '../..';

<Meta title="Components/Toggle/Radio" component={Toggle} />

export const Template = (args) => <Toggle {...args} />;

# Toggle

## Radio Toggle

Example of a `radio` toggle:

<Canvas>
  <Story
    name="Radio"
    args={{
      children: 'Radio toggle',
      id: 'id',
      value: 'value',
      type: 'radio',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
