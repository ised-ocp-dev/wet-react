import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Toggle } from '../..';

<Meta title="Components/Toggle/Active" component={Toggle} />

export const Template = (args) => <Toggle {...args} />;

# Toggle

## Active state

All styling variations in the `active` state:

<Canvas>
  <Story
    name='Default'
    args={{ disabled: false, children: 'Active toggle button', id: 'id', value: 'value',  variant: 'default' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Primary'
    args={{ disabled: false, children: 'Active toggle button', id: 'id', value: 'value',  variant: 'primary' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Success'
    args={{ disabled: false, children: 'Active toggle button', id: 'id', value: 'value',  variant: 'success' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Warning'
    args={{ disabled: false, children: 'Active toggle button', id: 'id', value: 'value',  variant: 'warning' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Danger'
    args={{ disabled: false, children: 'Active toggle button', id: 'id', value: 'value',  variant: 'danger' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Info'
    args={{ disabled: false, children: 'Active toggle button', id: 'id', value: 'value',  variant: 'info' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
