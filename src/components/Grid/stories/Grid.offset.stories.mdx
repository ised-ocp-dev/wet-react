import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { Grid } from '../..';

<Meta
  title="Components/Grid/Offset"
  component={Grid}
  subcomponents={{ 'Grid.Row': Grid.Row, 'Grid.Cell': Grid.Cell }}
/>

export const Template = (args) => (
  <Grid fluid>
    <Grid.Row>
      <Grid.Cell xsWidth={2} smWidth={2} mdWidth={2} lgWidth={2}>
        <div class="well well-sm brdr-rds-0">Cell 1</div>
      </Grid.Cell>
      <Grid.Cell {...args}>
        <div class="well well-sm brdr-rds-0">Edit my offset!</div>
      </Grid.Cell>
    </Grid.Row>
  </Grid>
);

# Grid Offset

One row has a width of 12. Use the `xsOffset`, `smOffset`, `mdOffset`, `lgOffset` attributes to specify the offset (out of 12)

that a given cell will have on a given viewport size (xs, sm, md, lg)

## example xsOffset

<Canvas>
  <Story name="xsOffset" args={{ xsOffset: 6, xsWidth: 3 }}>
    {Template.bind({})}
  </Story>
</Canvas>

## example smOffset

<Canvas>
  <Story name="smOffset" args={{ smOffset: 4, smWidth: 3 }}>
    {Template.bind({})}
  </Story>
</Canvas>

## example mdOffset

<Canvas>
  <Story name="mdOffset" args={{ mdOffset: 8, mdWidth: 3 }}>
    {Template.bind({})}
  </Story>
</Canvas>

## example lgOffset

<Canvas>
  <Story name="lgOffset" args={{ lgOffset: 3, lgWidth: 3 }}>
    {Template.bind({})}
  </Story>
</Canvas>
