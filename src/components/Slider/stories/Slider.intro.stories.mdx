import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import { Slider } from '../..';

<Meta title="Components/Slider/Intro" component={Slider} />

# Slider

The `<input type="range" />` element enables the display of a slider control bar.

Because some browsers do not support this functionality natively, this polyfill emulates the same functionality using generic HTML and WAI-ARIA.

## Example

<Canvas>
  <Slider min={0} max={100} value="20" step={2} title="Example Slider" />
</Canvas>

## Available Props

<ArgsTable />
