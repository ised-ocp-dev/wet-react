import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Tabs } from '../..';

<Meta
  title="Components/Tabs/Open"
  component={Tabs}
  subcomponents={{
    'Tabs.Panel': Tabs.Panel,
  }}
/>

export const Template = (args) => (
  <Tabs id="unique">
    <Tabs.Panel id="hi" title="tab one" children={<p>hello there</p>} />
    <Tabs.Panel
      {...args}
      id="hello"
      title="tab two"
      children={<p>obi-wan!</p>}
    />
  </Tabs>
);

# Tabs

## Open

Use the `open` attribute to set which tab to have open on pageload. It will select the first 'open' tab, or default to the first tab

## Example

<Canvas>
  <Story name="open" args={{ open: true }}>
    {Template.bind({})}
  </Story>
</Canvas>
