import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Tabs } from '../..';

<Meta
  title="Components/Tabs/IgnoreSession"
  component={Tabs}
  subcomponents={{
    'Tabs.Panel': Tabs.Panel,
  }}
/>

export const Template = (args) => (
  <Tabs {...args} id="unique">
    <Tabs.Panel id="hi" title="tab one" children={<p>hello there</p>} />
    <Tabs.Panel id="hello" title="tab two" children={<p>obi-wan!</p>} />
  </Tabs>
);

# Tabs

## Ignore Session

Use the `ignoreSession` attribute to open the default tab on page load, instead of the last opened tab

## Example

<Canvas>
  <Story name="Ignore Session" args={{ ignoreSession: true }}>
    {Template.bind({})}
  </Story>
</Canvas>
